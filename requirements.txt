# Local pipeline (Phases 1-3 + evaluation orchestration). Python 3.9+.
# Training deps (trl/peft/torch) live in training/requirements-train.txt — install on Colab.

google-genai>=0.3.0      # Gemini 2.5 client (new unified SDK)
pandas>=2.0
numpy>=1.24
pyarrow>=14.0            # parquet I/O for processed campaigns
python-dotenv>=1.0
tqdm>=4.66
pydantic>=2.5           # schema validation for recommendations / preference pairs
